# HWP v0.5.2 协议草案

## 1. 核心定义
- v0.5.2 是对 v0.5 协议的细节修复和优化，确保协议执行的一致性与健壮性。
- 核心新增功能包括：
  - **Blind Spot 强化**：v0.5.2 增加了更多的“盲点”设置，确保路径多样性。
  - **路径冗余检查**：为路径字段增加了冗余检测机制，防止无效路径占用计算资源。
  - **Expanded Variables**：扩展了 `variables` 字段的上下文，使其支持更多语义群体。

## 2. 协议字段定义

### 新增字段
- `variable_context`：扩展的上下文信息，允许 variables 的某些子集进行灵活配置。

### 变更字段
- `drift_rate` 的计算公式经过优化，改为更精确的加权平均方法。

### 删除字段
- `legacy_paths` 字段已被弃用，不再使用。

## 3. 协议实现规范
- **计算方法**：优化了熵值和漂移率的计算方式，减少了计算时间并提升了性能。
- **路径计算**：新加入了路径冗余检测，确保每条路径都有足够的差异性。

## 4. 协议合规性测试
- **新增测试用例**：对 `variable_context` 和 `path_redundancy` 增加了测试。
- **删除测试用例**：移除了对 `legacy_paths` 的测试用例。

## 5. 版本控制与指纹
- SHA256 校验：`sha256=<新版本的校验>`
- 版本说明：v0.5.2 主要解决了路径冗余和盲点处理问题，进一步提升了协议的可扩展性。

