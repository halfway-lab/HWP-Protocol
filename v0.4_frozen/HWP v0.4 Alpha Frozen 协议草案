# HWP v0.4 Alpha Frozen 协议草案

## 1. 协议核心定义

**HWP（Half Way Protocol）** 是一套用于生成 **非收敛型认知结构** 的标准协议，旨在为复杂认知模型提供一个灵活的框架，允许多路径、多张力的知识体系生成和演化。

### 核心原则：
1. **不收敛（No Closure）**：系统不能自动结束，必须保持演化状态。
2. **不单路径（No Single Dominance）**：没有唯一的路径或解答，每个节点都能向多方向发展。
3. **不评价（No Judgment）**：不做结论性判断，避免定论。
4. **必须可继续（Mandatory Continuation）**：每个节点都有后续路径，可以不断扩展。
5. **必须包含盲点（Blind Spot Required）**：每个节点需要包含未解答的部分，保持一定的盲区。

## 2. 协议字段结构

每个节点都包含以下核心字段：

- **questions**: 问题列表，用于引导认知结构。
- **variables**: 变量列表，用于表示节点的内容和相互关系。
- **paths**: 可能的路径集合，表示认知结构可能的演化方向。
- **tensions**: 张力列表，用于表示不同路径之间的冲突或对立。
- **unfinished**: 未完成部分，表示需要后续补充的内容或未知领域。

## 3. 合并与扩展

每个协议版本必须兼容之前的版本，支持向后兼容和扩展。新的路径、张力或变量必须在现有的结构基础上进行扩展，而不是推翻现有规则。

